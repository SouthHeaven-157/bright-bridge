# 项目协作说明

本项目为“赛博外星调酒”9:16 网页互动游戏 MVP。

网页应用源码位于 `web/`；Git、项目文档与开发日志位于仓库根目录。执行前端命令时使用 `web/` 作为工作目录。

## 开始工作前

1. 先阅读 [文档索引](docs/00-文档索引.md)。
2. 按当前任务阅读索引中对应的需求、技术、设计、执行与验收规范。
3. 查看 `开发日志/` 中日期最新的日志，确认已完成事项、遗留问题和下一步。
4. 开始当天工作时创建或更新 `开发日志/YYYY-MM-DD.md`。

## 工作约束

- 所有下载文件、生成文件、缓存及项目产物必须位于 D 盘；默认仅写入本项目目录。
- 执行任何下载或依赖安装前，先检查项目和 D 盘约定位置是否已存在可复用内容，并记录检查结果。
- 不在未确认需求时扩展 AI、数据库、经典鸡尾酒匹配、自动命名、情绪文案、Three.js 或真实流体模拟。
- 每次只完成一个可验证的小阶段；阶段通过验收后再进入下一阶段。
- 视觉元素保持可替换，组件不得依赖某一张临时占位图的固定尺寸或构图。
- 状态只能通过统一数据入口修改；视觉层只读取计算结果，不自行维护配方数值。
- 修改后进行与改动规模相称的验证，并把结果写入当天开发日志。
- 未经明确要求，不发布、不部署、不提交远程仓库。

## 标准文件路径

- 产品范围：`docs/01-开发需求.md`
- 技术与架构：`docs/02-技术规范.md`
- 视觉与交互：`docs/03-设计规范.md`
- 分阶段执行：`docs/04-执行步骤.md`
- 测试与验收：`docs/05-验收标准.md`
- Apple/macOS/Xcode：`docs/06-macOS与Xcode说明.md`
- 可选技术参考：`docs/09-reference-projects.md`
- 视觉资产替换：`docs/10-视觉资产替换规范.md`
- 流程与资产集中修改点：`docs/11-集中修改点索引.md`
- 配料参数：`docs/12-配料参数表.md`
- AI 酒名与 Drink Card：`docs/14-AI酒名与DrinkCard接入规范.md`
- 每日记录：`开发日志/YYYY-MM-DD.md`

`docs/09-reference-projects.md` 仅作为可选技术参考，不是强制实现模板。不要为了贴近参考仓库而增加依赖或限制更优实现；若其内容与当前项目规范冲突，以当前项目 `docs/` 和用户最新需求为准。

## 每次工作结束

当天日志必须包含：本次目标、完成事项、验证结果、未完成事项、下一步和阻塞项。若规范或需求发生变化，应先更新 `docs/`，再更新实现。
