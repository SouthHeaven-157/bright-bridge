# 2026-09-12 开发日志

## 本次目标

建立项目开发规范、阶段计划和每日记录机制，不进入代码实现。

## 完成事项

- 查阅《赛博外星调酒网页 MVP》需求文档。
- 创建 `docs/` 项目文档目录及文档索引。
- 整理产品范围、技术规范、视觉规范、执行步骤和验收标准。
- 补充 macOS 开发与 Xcode 使用边界说明。
- 创建根目录 `AGENTS.md`，写入文档路径和日常工作规则。
- 创建每日开发日志模板说明和本日日志。

## 验证结果

- 文档覆盖当前 MVP 的目标、非目标、技术结构、交互规则、视觉边界和验收条件。
- 执行计划拆分为六个可独立验证的小阶段。
- 所有新增文件位于 D 盘项目目录中。
- 本次未下载文件、未安装依赖、未创建应用代码。

## 未完成事项

- 尚未初始化 Next.js 项目。
- 尚未实现任何游戏组件或业务逻辑。
- 尚未执行浏览器或构建验证。

## 下一步

仅执行“阶段 0：项目基线”：检查可复用内容、初始化工程、创建 9:16 GameViewport 与基础主题，并验证开发环境。

## 阻塞与决策

- 当前无阻塞。
- Xcode 不作为网页 MVP 的必需工具；仅在未来 iOS 原生封装阶段使用。

## 补充工作：Git 初始化

### 本次目标

初始化本地 Git 仓库并建立项目忽略规则。

### 完成事项

- 确认项目此前不是 Git 仓库。
- 在项目根目录执行 `git init`。
- 创建根目录 `.gitignore`。
- 忽略依赖、Next.js 产物、通用构建产物、环境变量文件、缓存、临时文件、编辑器文件和系统文件。

### 验证结果

- `git rev-parse --is-inside-work-tree` 返回 `true`。
- Git 状态正常，现有项目文档显示为未跟踪文件，未产生意外改动。
- 已用 `git check-ignore -v` 验证 `node_modules`、`.next`、`.env`、`.env.local`、`dist`、临时文件和系统文件规则均命中。

### 未完成事项

- 尚未创建任何 Git 提交。
- 尚未配置远程仓库。

### 下一步

Git 初始化阶段已完成。下一阶段可开始检查本机可复用依赖，再决定是否初始化 Next.js 工程。

### 阻塞与决策

- 保留 Git 默认生成的初始分支名；未在没有明确要求的情况下改名或创建提交。

## 阶段 0：项目基线

### 本次目标

初始化网页工程，建立 9:16 游戏画布、基础视觉主题和可运行的首屏切片。

### 完成事项

- 将网页应用放置在 `web/`，保留仓库根目录的 Git、文档和日志结构。
- 使用 Vinext/Next.js、React、TypeScript 和 Tailwind 初始化工程。
- 安装项目依赖及需求指定的 Framer Motion。
- 将 npm 缓存设置为 D 盘项目内的 `.npm-cache/` 并加入忽略规则。
- 创建 `GameViewport`，实现响应式 9:16 居中画布。
- 建立深青黑、冷峻赛博酒吧基础主题。
- 添加静态的章鱼酒保、酒杯、八种配料、Flavor Profile 和 Serve 首屏占位。
- 更新页面标题、描述、语言标记和项目专用酒杯图标。
- 更新 `AGENTS.md` 与技术文档中的 `web/` 源码路径说明。

### 验证结果

- 生产构建成功，根路由 `/` 已生成。
- 本地开发服务运行于 `http://localhost:5173/`。
- 绕过系统代理后的本机请求返回 HTTP 200，并确认页面标题和首屏结构正确。
- `node_modules`、构建产物、D 盘 npm 缓存和运行时状态均被 Git 忽略。

### 未完成事项

- 当前首屏为不可操作的视觉基线，尚未实现真实数据、混合引擎、动态酒杯或拖拽。
- 尚未进行正式视觉资产制作与浏览器多尺寸截图检查。
- 尚未部署或创建 Git 提交。

### 下一步

进入阶段 1：定义 Ingredient、DrinkState 等类型，录入八种配料，实现纯函数 `mixEngine` 和比例边界验证。

### 阻塞与决策

- 系统代理会令普通 localhost 请求返回 502；绕过代理后本地服务正常返回 200，确认不是应用错误。
- Sites 辅助脚本在当前 Windows npm 包装环境中解析路径失败，已使用 D 盘本机 npm 可执行文件完成同等安装与构建。
- 本阶段不发布未完成的 MVP。

## 阶段 1：数据与混合引擎

### 本次目标

建立可复用的配料、饮品和场景状态类型，录入八种配料，并实现纯函数混合引擎与比例边界。

### 完成事项

- 查阅新增的参考项目文档，将其整理到 `docs/09-reference-projects.md` 并纳入文档索引。
- 在 `AGENTS.md` 中明确参考项目仅为可选技术参考，当前项目规范和用户最新需求优先。
- 创建 Ingredient、IngredientId、DrinkState、FlavorProfile、LiquidVisualState、DragState 和 SceneState 类型。
- 录入 Gin、Rum、Vodka、Lemon、Lime、Honey、Oolong Tea、Soda 八种配料的风味、酒精、颜色和液体视觉数据。
- 实现 `mixDrink`、`createDrinkState`、`setRecipe` 和 `incrementIngredient`。
- 实现配方合并、整数化、负值清理、未知配料防护和总量 100% 上限。
- 实现风味、酒精度、RGB、透明度、气泡与浑浊度的百分比加权计算。
- 添加五个聚焦数据层的自动化测试。

### 验证结果

- 空配方安全默认值测试通过，无 `NaN` 或无效颜色。
- 单配料映射测试通过。
- 多配料百分比加权测试通过。
- 整数递增和 100% 上限测试通过；满杯后新增增量不会挤占已有配料。
- 重复配料合并与负增量清空测试通过。
- 测试结果：5 项全部通过。
- TypeScript `--noEmit` 类型检查通过。
- 生产构建成功，现有根路由保持可用。

### 未完成事项

- 数据层尚未接入 React 页面状态。
- 尚未实现动态酒杯和 Flavor Profile 实时显示。
- 尚未实现拖拽、Pour Zone、计时器和 Serve。

### 下一步

进入阶段 2：实现 SVG `DynamicGlass` 和可读取真实 FlavorProfile 数据的全息面板，并用固定测试配方验证视觉联动。

### 阻塞与决策

- 未下载或复制参考仓库；当前阶段不需要额外依赖。
- 初次测试发现满杯后递增会因标准化顺序挤占后加入的配料，已改为按剩余容量接受增量并通过回归测试。
- 本阶段没有用户可见玩法变化，因此不发布未完成的 MVP。

## 大量推进：阶段 2–5

### 本次目标

将数据层接入完整 MVP 界面，连续完成动态酒杯、风味显示、拖拽倒酒、2.5D 场景、机械章鱼响应、Serve 和 Drink Card。

### 完成事项

- 创建 SVG `DynamicGlass`，由 DrinkState 驱动液面、RGB、透明度、浑浊度和气泡。
- 酒液保持自然透明与重力方向，不使用霓虹描边或赛博纹理。
- 创建实时 `FlavorProfile`，显示六项数值、数字读数和平滑进度变化。
- 创建八个可拖拽 `IngredientBottle` 和 `IngredientShelf`，显示每种配料当前百分比。
- 创建 `PourZone` 与 `PourStream`，进入区域后显示液流并开始倒酒。
- 使用 Pointer Events 统一鼠标与触控；处理 `pointermove`、`pointerup` 和 `pointercancel`。
- 实现唯一倒酒计时器，每 100ms 通过 `incrementIngredient` 增加 1%。
- 在离开区域、松手、取消、满杯、Serve 和组件卸载时清理计时器。
- 为酒瓶补充键盘操作：Enter 或空格每次添加 1%。
- 创建七层场景结构与 ±2/4/6/8/10px 轻量视差。
- 创建拆分式机械章鱼酒保，加入身体浮动、头部旋转、触手摆动、眼部呼吸和倒酒响应。
- 实现 Serve 条件、配方冻结、场景压暗和 1.5 秒结果转场。
- 创建 Drink Card，显示当前酒杯、已用配料、比例、六项风味和 `#001`。
- 注册 `read_drink_state`、`set_drink_recipe` 和 `serve_drink` 三个 WebMCP 工具；浏览器不支持时静默跳过。
- 更新技术规范中的 WebMCP 工具说明。

### 验证结果

- TypeScript `--noEmit` 类型检查通过。
- mixEngine 回归测试 5 项全部通过。
- Vinext/Next.js 生产构建成功。
- 本地开发页面返回 HTTP 200。
- 使用本机 Edge 生成 500×900 竖屏 QA 截图，确认章鱼、酒杯、八种配料、Flavor Profile 和 Serve 均完整可见。
- 截图初查发现章鱼偏右和控制区裁切，已修复 Motion/CSS 居中冲突并改用可收缩布局，复查通过。

### 未完成事项

- 尚未在真实触屏设备上执行连续拖拽、快速进出 Pour Zone 和 `pointercancel` 手工验收。
- 尚未用浏览器自动化完整走通“拖瓶 → 倒酒 → Serve → Drink Card”；当前已通过类型、单元、构建和静态渲染检查。
- 当前环境未提供可调用的 WebMCP 验证上下文，因此只完成特性检测、注册代码和构建验证。
- 尚未替换正式美术资产，仍使用结构可替换的 SVG/CSS 占位视觉。
- 尚未发布或创建 Git 提交。

### 下一步

进入阶段 6：执行真实交互验收，重点检查鼠标/触控拖拽、计时器即时停止、满杯边界、结果转场、窄屏布局和浏览器控制台；仅修复验收中发现的问题。

### 阻塞与决策

- 用户明确要求本轮“大量推进”，因此连续完成阶段 2–5，而不是停在单个阶段。
- 未下载参考仓库或新增依赖，所有实现沿用现有 React、Framer Motion 和 SVG 能力。
- Headless Edge 的小窗口截图使用桌面最小布局宽度并发生图像裁切，不作为真实移动端失败结论；最终用完整 500×900 画布完成静态视觉复核，真实窄屏交互保留到阶段 6。
- 一次 QA 将 Edge 临时配置放入 `web/`，导致 Vite 监听锁文件并退出；相关临时目录已删除，后续 QA 配置改放仓库根目录 `.qa/`。
- 当前 MVP 尚未完成阶段 6，因此不发布。

## 布局与可替换资产修订

### 本次目标

先检查用户最新布局与资产要求是否和现有规范/代码冲突，先更新规范，再修改界面并接入首批自行生成资产。

### 冲突检查

- 直接规范冲突：`docs/03-设计规范.md` 原先规定下部同时放置八种酒瓶、Flavor Profile 和 Serve；最新要求改为 Flavor Profile 位于酒杯右侧。
- 直接代码冲突：`GameViewport.tsx` 原先在底部 `readout-row` 中并列渲染 Flavor Profile 和 Serve。
- 无冲突项：现有 SVG/CSS placeholder、七层场景、组件分离和可替换资产原则与最新要求一致。
- 处理决策：以最新用户要求为准，先修订需求、技术、设计、执行与验收文档，再修改实现；生成图作为独立增强层，不能替代 SVG/CSS 回退。

### 完成事项

- 将 Flavor Profile 从底部控制区移动到酒杯右侧，并保持六项数据实时联动。
- 缩短底部控制区，使八个配料瓶成为主要内容；Serve 改为独立紧凑操作条。
- 创建 `web/lib/visualAssets.ts` 集中管理可替换视觉资产路径。
- 使用内置图像生成模式制作 9:16 外星酒吧远景和透明机械章鱼酒保；生成前确认项目内不存在同名资产，生成后复制到 D 盘项目目录，未覆盖旧文件。
- 背景图作为远景增强层接入，CSS 星空、城市、窗口与酒架继续作为回退/分层细节。
- 透明章鱼图作为角色增强层接入；图片失败时隐藏图片并恢复 CSS 拆分式机械章鱼。
- 八个酒瓶继续使用结构化 CSS、酒杯继续使用动态 SVG、结果卡底板继续使用 CSS，确保整个 MVP 不依赖正式美术资产。
- 新增 `docs/10-视觉资产替换规范.md`，并更新文档索引与 `AGENTS.md` 标准路径。

### 生成提示词

1. 远景：9:16 赛博外星鸡尾酒吧独立环境背景，包含轨道窗、外星城市、酒架与氛围层；深青黑/石墨色、少量琥珀和蓝紫光；中央与右侧留 UI 空间；不含角色、酒瓶、酒杯、前景吧台、UI、文字、标识或水印。
2. 角色：透明背景、完整轮廓的机械章鱼酒保像素角色；深青与石墨机械材质、琥珀眼睛、少量蓝紫点缀；不含酒吧、道具、酒瓶、酒杯、UI、文字、标识或水印。

### 资产路径

- `web/public/assets/scene/alien-bar-background-v1.png`
- `web/public/assets/octopus/mechanical-bartender-v1.png`
- `web/lib/visualAssets.ts`

### 验证结果

- 章鱼资产为 32-bit ARGB PNG，保留透明通道；背景为独立竖屏 PNG。
- TypeScript `--noEmit` 类型检查通过。
- mixEngine 回归测试 5 项全部通过。
- Vinext/Next.js 生产构建成功。
- 本地页面返回 HTTP 200。
- Edge 500×900 最终截图确认：生成角色无双影、酒杯完整、Flavor Profile 位于右侧且不遮挡 Pour Zone、八个瓶子和 Serve 完整可见。

### 未完成事项

- 尚未制作八个正式酒瓶、正式酒杯和正式结果卡底板美术；当前 CSS/SVG placeholder 已满足 MVP。
- 尚需在真实触屏设备上完成连续拖拽、快速进出 Pour Zone 和 `pointercancel` 验收。
- 图片加载失败回退已实现代码路径，后续阶段 6 仍需加入浏览器断图手工验收。

### 下一步

进入阶段 6 的真实交互与窄屏验收；若用户提供正式资产，严格按照 `docs/10-视觉资产替换规范.md` 逐槽位替换。

### 阻塞与决策

- 当前无阻塞。
- 未新增依赖、未下载外部仓库、未发布、未部署、未创建 Git 提交。

## 阶段 6：可重复验收

### 本次目标

完成阶段 6 的可重复自动验收，覆盖数据测试、浏览器端到端流程、Pointer Events、窄屏布局和视觉资产回退；只修复验收入口发现的阻断问题。

### 完成事项

- 为 `web/package.json` 增加 `test:mix`，固定使用项目已有的 `tsx` loader 执行 TypeScript ESM 测试。
- 修复 `web/scripts/browser-smoke.mjs` 的浏览器启动参数，增加 `--no-sandbox` 以适配当前 Windows 验收环境。
- 在 browser smoke 中等待 React hydration 完成后再派发键盘事件，避免 SSR DOM 已出现但事件处理器尚未挂载造成误判。
- 完成键盘加料 24% → Serve → Drink Card → `MIX AGAIN` 端到端流程。
- 完成 Pointer Events 拖拽进 Pour Zone、持续倒酒、离开区域立即停止、释放后酒瓶回位和 Serve 流程验证。
- 完成 512×746 浏览器视口布局检查：无横向溢出，8 个酒瓶、酒杯、右侧 Flavor Profile 与 Serve 均在视口内。
- 模拟背景图和章鱼图加载失败，确认 CSS/结构化占位回退仍存在，8 个酒瓶和 Serve 状态不受影响。

### 验证结果

- `npm.cmd run test:mix`：5 项通过。
- `tsc.cmd --noEmit`：通过。
- ESLint：0 errors，保留 1 条 `BarScene.tsx` 原生 `<img>` 性能 warning。
- `node scripts/run-framework.mjs build`：生产构建成功。
- `node scripts/browser-smoke.mjs`：通过；结果卡保留 Gin 24%，`MIX AGAIN` 后回到 0%。截图输出 `.qa/browser-smoke-result.png`。
- Pointer smoke：倒酒期间 3%，离开 Pour Zone 后保持 3% 且停止，释放后 active 状态清除，Serve 结果卡正常。
- 窄屏布局 smoke：scrollWidth 等于 viewport width，无横向滚动；关键元素边界均在视口内。
- 资产回退 smoke：背景与角色增强层隐藏并切换到 placeholder，8 个酒瓶保留，空杯时 Serve 仍禁用。

### 未完成事项

- 尚未在真实 iOS/Android 触屏设备上完成手指拖拽和系统级 `pointercancel` 手工验收。
- 尚未制作正式酒瓶、正式酒杯和正式结果卡底板美术；当前 SVG/CSS placeholder 已支撑完整 MVP。
- WebMCP 仍未在真实支持 `document.modelContext` 的浏览器中实测，仅完成特性检测、注册代码和构建验证。
- 尚未创建 Git commit，也未配置远程仓库。

### 下一步

阶段 6 的桌面浏览器自动验收已通过。下一步最适合在真实触屏设备或用户提供正式视觉资产后，按 `docs/10-视觉资产替换规范.md` 逐槽位验收；在此之前不扩展玩法或引入新依赖。

### 阻塞与决策

- 当前无功能阻塞。
- 原始 Node 测试命令因无扩展名 TypeScript ESM import 无法直接运行，已通过项目已有 `tsx` 依赖建立稳定测试脚本，未新增依赖。
- 浏览器 smoke 需要 `--no-sandbox` 才能在当前验收环境启动 Chrome；该参数仅用于本地测试脚本，不影响应用运行。

## MVP 收尾：阶段 6 加固

### 本次目标

将阶段 6 验收发现的阻断项修复并收敛为可重复的 MVP 验收入口。

### 完成事项

- 倒酒计时器使用当前拖拽 session 与实时 DragState 校验，离开 Pour Zone 时同步清理，避免 stale tick；倒酒区域按拖拽瓶身移动后的边界判定。
- 拒绝 secondary pointer、非主指针和非左键鼠标，补充 `lostpointercapture`、窗口失焦与页面隐藏清理。
- 修复移动端 9:16 画布约束，增加安全区内边距；酒瓶区改为 2×4 网格，确保窄屏触控目标达到 44px 以上。
- 保留完整 `Oolong Tea` 标签，放宽酒瓶文字尺寸；收窄 Pour Zone 与 Flavor Profile 的几何范围，保留明确间隔。
- 使用 `MotionConfig reducedMotion="user"` 接管 Framer Motion 的 reduced-motion 行为。
- 结果卡增加 dialog 语义、结果出现后的焦点转移，并关闭持续百分比播报，避免倒酒时屏幕阅读器被高频打扰。
- WebMCP `set_drink_recipe` 仅允许在 mixing 状态修改配方，维护 Serve Promise 的完成/取消引用。
- 浏览器 smoke 纳入 390×844 移动布局检查、9:16 比例、横向溢出、Pour Zone/Flavor Profile 间隔和触控目标尺寸。

### 验证结果

- `npm.cmd run test:mix`：5 项通过。
- TypeScript `--noEmit`：通过。
- ESLint：0 errors，1 条既有原生 `<img>` 性能 warning。
- Pointer smoke：通过拖拽、持续倒酒、离开区域停止、释放回位和 Serve。
- Asset fallback smoke：通过。
- Mobile-aware browser smoke：通过 390×844；画布 390×693.33（9:16），scrollWidth 390，酒瓶触控目标 90.94×79.86，Pour Zone 与 Flavor Profile 无重叠，8 个酒瓶存在；Serve、结果卡、MIX AGAIN 全流程通过。
- Vinext 生产构建：成功。
- 本地开发页：HTTP 200。

### 未完成事项

- 尚未在真实 iOS/Android 触屏设备上手工验证系统级 pointercancel、safe-area 实际设备开孔和浏览器差异。
- WebMCP 仍未在真实支持 `document.modelContext` 的浏览器中实测。
- 正式酒瓶、正式酒杯和正式结果卡资产仍未制作；当前 SVG/CSS placeholder 已满足 MVP。
- 尚未创建 Git commit，也未配置远程仓库。

### 下一步

MVP 的桌面与移动模拟验收已通过。下一步仅在真实触屏设备或获得正式视觉资产时继续，不扩展 AI、数据库、联网或其他非 MVP 功能。

### 阻塞与决策

- 当前无功能阻塞。
- 当前开发服务曾因并发编辑触发 Vite Windows watcher 的 `EBUSY` 并退出；重新启动后页面正常，属于本地 watcher 环境问题，不是构建或应用运行错误。

## MVP 收尾提交

### 本次目标

完成当前 MVP 的 Git 收尾，不继续开发新功能。

### 完成事项

- 确认仓库已在项目根目录初始化，当前分支为 `master`。
- 核对根目录与 `web/.gitignore`：依赖、构建产物、测试截图、环境变量、运行时状态和本地缓存均被忽略。
- 确认当前 MVP 源码、文档、资源和开发日志均纳入首次稳定提交范围。
- 将 TypeScript 增量构建缓存 `web/tsconfig.tsbuildinfo` 从暂存区移除，并在根 `.gitignore` 增加 `*.tsbuildinfo` 规则。
- 创建当前 MVP 的首次稳定 Git 提交：`5de5db4`（`feat: 赛博外星调酒 9:16 互动 MVP`）。

### 验证结果

- Git 仓库有效：`rev-parse --is-inside-work-tree` 返回 `true`，分支为 `master`。
- 提交包含 141 个文件；提交后 `git status` 干净，无未跟踪或未提交改动。
- 提交前已确认 `node_modules`、`.next`、`dist`、`.qa`、`.env.local`、`.sites-runtime`、`.wrangler`、`*.tsbuildinfo` 等本地产物不会进入提交。
- 仓库此前未配置提交身份，本次仅设置仓库级 `user.name` / `user.email`（`情绪酒馆 Dev <dev@localhost>`），未修改全局 Git 配置；如需署名请自行替换。
- 本次不新增玩法、不部署、不配置远程仓库。

### 未完成事项

- 真实移动设备手工验收、真实 WebMCP 宿主验证和正式美术资产仍属于 MVP 之外的后续工作，不影响当前稳定提交。

### 下一步

当前 MVP 收尾完成；后续如需继续，仅在新的明确需求下开始新阶段。

### 阻塞与决策

- 当前无阻塞。
- API 密钥不是本项目 MVP 的运行依赖；项目未接入 AI/API 服务。若外部工具提示“API 密钥无效”，应检查该工具自身的环境变量、密钥来源、过期状态和账户权限，不属于本项目代码错误。

## 恢复稳定基线与交接阶段

### 本次目标

停止未完成的背景、酒瓶、杯型与酒杯落桌回滚实验，优先恢复到正常可运行的最近稳定 MVP，并生成后续 agent 可直接接手的对接文档。

### 完成事项

- 将工作区恢复到最新稳定提交 `e82cd52`，撤销所有未提交实验改动和未跟踪实验文件。
- 保留原始 MVP 的底部 Ingredient Array、基础动态酒杯、Flavor Profile、拖拽倒酒、Serve、Drink Card 和 WebMCP 代码。
- 新增 `docs/13-交接文档-恢复正常运行.md`，记录稳定基线、运行命令、验证顺序、已撤销范围和后续协作纪律。
- 在文档索引中加入交接文档入口。

### 验证结果

- `git status --short`：恢复后为空（新增交接文档和索引/日志更新后会产生本阶段文档改动）。
- 已确认最新稳定提交为 `e82cd52`，其父提交 `5de5db4` 为 MVP 首次稳定实现。
- 已确认实验性的 `web/components/glass/`、`web/components/stream/`、杯型 preset、ingredient catalog 等未跟踪文件已清理。
- 类型、构建和 HTTP 200 验证将在交接前完成。

### 未完成事项

- 不再继续本轮视觉或布局调整。
- 需要后续 agent 在接手时重新执行 TypeScript、构建和本地 HTTP 验证。

### 下一步

保持当前稳定基线；只有收到新的明确需求后，按单一小阶段重新开始。

### 阻塞与决策

- 当前无应用功能阻塞。
- 本次优先选择可运行性和可交接性，不继续做无提交阶段之间的精准合并。
