# 2026-09-13 开发日志

## BrightBridge 整合阶段

- 本次目标：将智触心桥与情绪酒馆合并为单一 Vinext 项目，并推送到用户指定的 `SouthHeaven-157/bright-bridge`。
- 已完成：单域路由为 `/bridge` 智触心桥、`/` 情绪酒馆；迁移 BrightBridge 前端、课程生成/视觉 API、响应式样式与独立 Logo；语音命中集中触发词后同域跳转 `/`。
- 验证结果：`npx tsc --noEmit` 通过；`npm run build` 五阶段通过；`/`、`/bridge`、`/api/bridge/health` 均返回 200；DeepSeek-V4.1-Flash 真实生成课程成功，HTML 长度 15539。
- 未完成：真实麦克风权限与移动端视觉识别尚需不同浏览器/设备验收。
- 下一步：推送目标 GitHub 仓库，部署时在平台配置 AI 密钥。
- 阻塞项：无代码阻塞；本地密钥文件已确认被 Git 忽略。

## 本次目标

按用户最新 12 项需求升级交互结构：酒瓶进入后方酒柜、底部改为杯型选择、增加 Pour Focus 与中文配料提示、扩展动态酒液表现，并增强 Drink Card 信息可视化。

## 当前基线与约束

- 从稳定提交 `e82cd52` 开始，保留现有未提交的交接文档与索引/旧日志更新，不覆盖无关改动。
- 用户明确允许本阶段大量推进，但所有流程参数与资产路径仍必须保留集中修改入口。
- 本轮用户累计提供 25 张视觉资产；每批均先检查现有文件再复制到 D 盘项目，原文件不删除、不覆盖。

## 完成事项

- 已读取项目索引、需求、技术、设计、执行、验收、资产替换和集中修改点规范。
- 已确认最新需求将替代“底部 Ingredient Array”的旧布局，属于明确产品结构变更。
- 已先更新 `docs/01`～`05`、`docs/10`、`docs/11`，统一记录酒柜入口、四种杯型、Pour Focus、动态酒液和结果卡配方分段规范。
- 已将用户提供的 25 张图片保留原件并复制到 `web/public/assets/user-provided/2026-09-13/`，使用语义文件名接入；未覆盖旧版背景和章鱼资产。
- 已把背景、吧台前景、19 个瓶身和四种杯具路径集中到 `web/lib/visualAssets.ts`；图片加载失败时继续显示 CSS/SVG 回退层。
- 已将八个瓶子从底部移入场景后方酒柜。静态时位于机械章鱼之后，拖拽时酒瓶所在交互层升到前景；酒保展示层不拦截瓶子 Pointer Events。
- 已把底部改为 highball、tumbler、cocktail、collins 四种杯型选择，杯型只传给 `DynamicGlass` 和 `DrinkCard`，不进入 mixEngine。
- 已扩展动态酒液数据与表现：容量、混色渐变、透明度、浑浊、粘度、气泡、泡沫、液面波纹、入流冲击与扩散；属性继续由配方权重计算。
- 已实现 Pour Focus：真正进入 Pour Zone 后杯子放大、背景和酒保弱化、当前酒瓶变前景、杯旁显示中文配料名；松手立即退出并恢复。
- 已让 Flavor Profile 平时弱化、倒酒时增强，并以琥珀色突出当前配料影响最大的风味维度。
- 已增强 Serve 揭晓演出，并让 Drink Card 显示最终杯型、中英配料名、比例、六维风味和配方分段信息图。
- 已增强浏览器验收脚本：检查三页酒柜、新增资产、底部 0 瓶、4 个杯型、背景原桌面、薄型 HUD、常态杯具、Pour Focus 动态 SVG、持续倒酒、松手恢复、Serve、结果卡与重置，并保存三阶段截图到 `.qa/`。
- 根据后续反馈重排酒柜：全部位于左侧，单页按 4 列 × 3 层共 12 个视觉槽位规划；19 种配料按 8 / 8 / 3 分成三页，避免单页过密。
- 机械章鱼整体下移到右侧工作位，八瓶与名称在常态截图中均无遮挡；酒保图层继续保持 `pointer-events: none`，不影响后方瓶子拖拽。
- 主酒杯上移并落在吧台桌面，底部控制区只保留四种杯型缩略图和 Serve `SERVE`。
- 已将 highball、tumbler、cocktail、collins 四张用户杯具图复制到 `web/public/assets/user-provided/2026-09-13/glassware/`，并由 `visualAssets.glassware` 集中管理。常态显示杯具资产，真正倒酒时淡出位图并显示动态 SVG，松手恢复；图片失败时 SVG 自动回退。
- 已撤销覆盖背景原桌面的常态 CSS 前台：背景成功时直接显示原图自带木质桌面，CSS `.counter` 只在背景失败时回退；杯底对齐桌面中段。顶部 HUD 约 38px，底部杯型与 Serve 同行并压缩到约 59px，按钮仍保持 44px 命中。
- 已为新增 11 种调料建立完整风味、酒精度、颜色、透明度、气泡、浑浊度、黏度和泡沫参数，参数口径记录在 `docs/12-配料参数表.md`。
- 已撤销覆盖背景原桌面的常态 CSS 前台：背景加载成功时直接显示原图自带木质桌面，CSS `.counter` 只在背景失败时回退；杯底对齐桌面中段。顶部 HUD 压缩到约 38px，底部杯型与 Serve 同行并压缩到约 59px，按钮仍保持 44px 命中。
- 已为 Campari、Sweet Vermouth、Dry Vermouth、Simple Syrup、Tonic Water、Ginger Beer、AD 钙奶、旺仔牛奶、豆浆、桂花酒、青梅酒建立完整参数，口径记录在 `docs/12-配料参数表.md`。
- 用户提供的 `bar-counter-foreground.png` 已改为 Pour Focus 专用图层：常态透明度为 0，倒酒特写时淡入；未新增流程状态。
- 倒酒中文配料提示移到酒杯左侧，避免与右侧 Flavor Profile 叠压。
- 根据用户对层级与比例的复核，进一步把 Pour Zone 碰撞区与常态杯显示尺寸解耦：常态杯可见宽度压缩到场景约 18%，杯底落在场景约 66.4% 的原图木质桌面，并增加独立椭圆接触阴影。
- 常态酒柜层降到机械酒保之后，桌面酒杯保持近景但不再压过角色；常态杯资产降低对比度和不透明度，隐藏未倒酒时的动态 SVG 残影与悬浮 Pour Zone 文案。
- 拿起酒瓶时由陈列尺度放大到 1.9 倍，进入 Pour Focus 后放大到 2.6 倍并隐藏按钮文字/边框；浏览器实测前景瓶高度为特写杯的约 101%，不再是小缩略图。
- 恢复并增强倒酒可视反馈：液流延长到杯口、增加流动液滴、低液位冲击环与冲击柱；透明基酒也能看到连续液流、液面波纹和扩散反馈。
- 按用户最新截图移除 Pour Zone 的蛋壳形虚线外圈和整块区域底色；碰撞区继续保留为不可见命中层，倒酒反馈只由瓶子、液流、杯体、中文名称和 Flavor Profile 承担。
- 杯旁 Flavor Profile 六维名称已统一改为甜度、酸度、苦度、酒精感、清新度、酒体，并把面板宽度提高到场景约 33.7%、常态不透明度提高到 0.78。
- 新增“忧郁—喜悦”双向情绪层：19 种配料各自具有 `moodValence (-1..1)`，mixEngine 按实际百分比加权；界面显示深沉、静思、平衡、明快、欢悦五档，参数依据和调酒师判断记录于 `docs/12-配料参数表.md`。
- 倒酒特写改为固定前景瓶姿态：酒瓶瓶口朝杯口，原酒柜拖拽瓶在特写中隐藏；液流由直线升级为从瓶口出发、弯入杯口的贝塞尔弧线，同时保留液滴、冲击和液面动画。
- 按新增配料陈列要求将 19 种配料拆为两个唯一数据派生集合：14 种常规配料保留在左侧酒柜并分为 8 / 6 两页，AD 钙奶、旺仔牛奶、豆浆、桂花酒、青梅酒固定陈列在前台左侧原图桌面，不复制配料参数。
- 酒柜与前台陈列均隐藏瓶下中文名和 `0%`，保留按钮 `aria-label`；中文配料名只在真正进入 Pour Focus 后显示在杯旁。
- 移除酒柜交互容器的矩形/圆角边框、黑色蒙层、内阴影、CSS 假层板及可见预留槽；交互命中区保持不变，瓶子直接贴合背景资产已有三层木架。
- 单独上调酒柜第二排瓶底，使其贴合木架承托线；隐藏瓶后 CSS 占位图重影。前台五瓶采用 3 + 2 的前后排陈列，并整体上移到原图桌面透视区域，未覆盖为新的桌子图层。

## 验证结果

- 按最新反馈取消配料分页与前台配料层：19 瓶现已全部同时混排在左侧三层木架，分布为 7 / 6 / 6；AD 钙奶、旺仔牛奶、豆浆、桂花酒和青梅酒均已进入酒柜。
- 三层瓶底分别校准到背景木架承托线，第三层额外上移约 6px；保留不规则横向错位，前台桌面不再放配料。
- 最新 `npx tsc --noEmit` 与 `npm run test:browser` 通过；浏览器实测酒柜 19 瓶、分页 0、前台配料 0，AD 钙奶拖拽、中文提示、弧线倒酒与松手停止均正常。

- `npx tsc --noEmit`：通过。
- `npm run test:mix`：8/8 通过，新增验证 19 个唯一 ID、所有归一化/RGB 参数边界以及碳酸与乳浊配料加权。
- `npm run build`：通过，vinext 五阶段客户端/服务端构建完成。
- `npm run test:browser`：最终回归通过；390×844 无横向滚动，9:16 画布正确；三页酒柜分别为 8 / 8 / 3 瓶，第二页金巴利资产与第三页豆浆、桂花酒、青梅酒均可访问；背景加载成功且 CSS 假桌面透明度为 0；顶部约 38px、底部约 59px、杯型按钮 44px；Pointer Pour 增长到 8%，松手仍为 8%，Serve 后 32%，重置回 0%。
- 视觉 QA：`browser-smoke-mixing.png` 中背景原有木质桌面完整可见，杯具资产杯底位于桌面中段；八瓶集中于左侧酒柜，右侧酒保无遮挡，顶部和底部为薄型 HUD；Pour Focus 和结果卡截图保持正常。
- 本轮视觉回归：常态杯宽/场景宽为 `17.997%`、高/场景高为 `16.904%`、杯底位置为 `66.391%`，接触阴影不透明度 `0.88`；Pour Focus 中瓶/杯高度比 `1.01`，液流与杯口相交，2 个冲击效果节点存在。
- 本轮 `npx tsc --noEmit`、`npm run test:mix`（8/8）、`npm run test:browser` 与 `npm run build` 均通过；构建仍只有已知的 vinext 路由分类提示。
- 移除 Pour Zone 外圈后再次通过 `npx tsc --noEmit` 与 `npm run test:browser`；回归确认特写状态边框为透明、背景图层为 `none`，同时瓶/杯比约 `1.017`、液流相交与松手停止保持正常。
- 中文风味/情绪/弧线倒酒最终回归：六维中文存在且无英文维度残留，情绪轴存在；面板宽度约 33.7%；前景瓶/杯高比约 0.915，液流包含曲线路径并与杯口相交；松手后 `8% -> 8%` 保持不变。
- 本轮再次通过 `npx tsc --noEmit`、`npm run test:mix`（8/8）、`npm run test:browser` 与 `npm run build`；构建仍只有已知 vinext 路由分类提示。
- 最新陈列回归再次通过 `npm run test:browser`：首屏共 13 个可拖拽瓶（酒柜 8 + 前台 5），酒柜共 2 页且第二页为 6 瓶；瓶下可见文字为 0，酒柜容器背景为 `none`、边框 `0px`、阴影 `none`；前台五瓶全部位于左侧原图桌面范围。
- 浏览器补验前台 AD 钙奶拖拽成功：Pour Focus 中文提示为“正在倒入 AD 钙奶”，使用对应前景资产并隐藏原陈列瓶；酒柜金酒倒酒保持贝塞尔弧线、2 个冲击效果、松手后 `8% -> 8%` 不再增长。
- 最新 `npm run build` 通过，vinext 五阶段构建完成；仅保留已知的路由静态分类提示。
- 定向执行 `npx eslint app components lib types scripts --ignore-pattern .qa`：0 error、3 个既有原生 `<img>` 性能提示。全目录 `npm run lint` 会扫描 `web/.qa/fallback-profile` 中由浏览器生成的第三方扩展压缩脚本并失败，该结果不属于项目源码错误；本轮未删除 QA 证据目录。
- 生产构建只出现 vinext 对动态路由静态分类能力的提示，不影响当前单页构建。

## 未完成事项

- 用户当前提供的酒瓶 PNG 带黑色背景，MVP 已利用暗色酒柜和 screen 混合接入；正式透明瓶身资产到位后仍建议替换。
- 尚未在真实 iPhone Safari、macOS Safari 与 Xcode Simulator 进行触控、刘海安全区和性能验收。
- 当前酒液是可控 SVG/CSS 动态视觉，不是 WebGL 真实流体；符合当前 MVP 边界。

## 下一步

下一阶段优先做真实 iPhone / Safari / Xcode Simulator 验收与细节调优；若先收到正式透明资产，则仅通过 `web/lib/visualAssets.ts` 和表现层样式替换，不动交互和混合引擎。

## 阻塞与决策

- 当前无代码阻塞。
- Windows 沙箱会阻止 Node/Vite/Edge 创建子进程，因此 mix 测试、生产构建和浏览器 QA 经授权在沙箱外运行；所有读写仍限定于 D 盘项目目录。
